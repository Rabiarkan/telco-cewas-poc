services:
  postgres:
    image: postgres:16
    container_name: cewas_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20

  mlflow:
    build:
      context: .
      dockerfile: Dockerfile.mlflow
    container_name: cewas_mlflow
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - mlruns:/mlruns
    command: >
      mlflow server
      --backend-store-uri ${MLFLOW_BACKEND_STORE_URI}
      --default-artifact-root ${MLFLOW_DEFAULT_ARTIFACT_ROOT}
      --host 0.0.0.0
      --port 5000


  app:
    image: python:3.11-slim
    container_name: cewas_app
    depends_on:
      postgres:
        condition: service_healthy
      mlflow:
        condition: service_started
    env_file:
      - .env
    working_dir: /work
    volumes:
      - ./:/work
    command: bash -lc "pip install -r requirements.txt && tail -f /dev/null"

  jupyter:
    build:
      context: .
      dockerfile: Dockerfile.jupyter
    container_name: cewas_jupyter
    ports:
      - "8888:8888"
    env_file:
      - .env
    environment:
      - JUPYTER_TOKEN=${JUPYTER_TOKEN}
    volumes:
      - ./:/home/jovyan/work
      - jupyterdata:/home/jovyan/.jupyter
    depends_on:
      postgres:
        condition: service_healthy


volumes:
  pgdata:
  mlruns:
  jupyterdata:


